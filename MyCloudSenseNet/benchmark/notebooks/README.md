# Notebooks

Exploration and demo notebooks live here. Keep production training, inference,
configuration, model code, and reusable utilities in the benchmark package:

- `benchmark/configs/`
- `benchmark/models/`
- `benchmark/training/`
- `benchmark/inference/`
- `benchmark/utils/`
